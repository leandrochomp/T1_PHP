1- config.ini
Adaptador: é o tipo de banco a ser usado no projeto.
Usuario / Senha: Informações referentes para acessar o banco

2- Params ; DBName e config.ini

3 - autoload.php

4 - Zend_DB_Select($db)
	$select->from('livros');

5 - $select->from ('livros');
	$where = ('ano < 2009');
	$select->where ($where);

6 - $element = new Zend_Form_Element_Text('titulo');
	$element->setLabel('Titulo');
	$element->setValue($titulo);
	$form->addElement($element);

7 - 	class Turmas extends Zend_Db_Table_Abstract {
				protected $_name = 'livros';
		}

8 - $livros = new Livros();
	$registros = $livros->fetchAll();

9 - $livros = new Livros();
    $_livro = $livros->find($id)->current();
    $_livro->delete();

10 - As dificuldades foram utilizar os forms no padrão do zend e o mapeamento relacional. 